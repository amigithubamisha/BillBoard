{
   "version": 2,
   "builds": [
      {
         "src": "./index.js",
         "use": "@vercel/node"
      }
   ],
   "routes": [
      {
         "src": "/api/(.*)", // Change this to match your Express routes
         "dest": "/index.js" // Point to your Express application entry point
      },
      {
         "src": "/(.*)",
         "dest": "/index.js" // Point to your Express application entry point
      }
   ]
}
